# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.4.2
pydantic-settings==2.0.3

# NLP & ML dependencies
sentence-transformers==2.2.2
faiss-cpu==1.13.2  # Latest available on PyPI. For GPU: use conda to install faiss-gpu
numpy==1.24.3

# LLM dependencies
torch==2.0.1
transformers==4.34.0

# Data processing
requests==2.31.0
beautifulsoup4==4.12.2
pdfplumber==0.10.3

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6

# Development dependencies (optional, comment out for production)
# pytest==7.4.3
# pytest-asyncio==0.21.1
# black==23.11.0
# flake8==6.1.0

# GPU Support Notes:
# FAISS: faiss-gpu is not available on PyPI. To use GPU-accelerated vector search:
#   1. Install conda: https://docs.conda.io/en/latest/miniconda.html
#   2. Create conda env: conda env create -f environment-gpu.yml
#   3. Activate: conda activate travel-rag-gpu
#   4. faiss-gpu will be installed via: conda install -c conda-forge faiss-gpu
# PyTorch and Transformers will use CUDA if a CUDA-enabled build is installed
